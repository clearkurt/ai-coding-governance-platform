# 嵌入式 C 代码规范（MVP 示例）

- 使用 C99，缩进为 4 个空格；花括号独占一行。
- 宏使用全大写加下划线，函数使用 `snake_case`。
- 寄存器、LCD SEG/COM 映射必须以具名宏或枚举表达，避免散落的魔法数字。
- 每个外部函数应有简短中文注释，参数和返回值含义清晰。
- 不得在中断中执行阻塞操作或动态内存分配。
- 输出应包含修改说明、代码草案及硬件相关的待确认事项。
